These files are meant to suppress typescript errors for certain packages by defining their type,
but this doesn't work for mol3d or miew. Instead the errors are suppressed using // @ts-ignore
The shims-vue.d.ts does work.